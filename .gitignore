.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/sdks
!.yarn/versions

# Whether you use PnP or not, the node_modules folder is often used to store
# build artifacts that should be gitignored
node_modules

# Swap the comments on the following lines if you wish to use zero-installs
# In that case, don't forget to run `yarn config set enableGlobalCache false`!
# Documentation here: https://yarnpkg.com/features/caching#zero-installs

#!.yarn/cache
.pnp.*

# Dependencias (pesan mucho, se instalan solas en el servidor)
node_modules/

# Carpeta de compilación (se genera sola)
dist/

# Variables de entorno (SECRETOS - Nunca subir esto)
.env

# Archivos del sistema operativo
.DS_Store
Thumbs.db

# Logs de errores
npm-debug.log
yarn-error.log

# Carpeta de uploads locales (ya usamos Cloudinary)
uploads/

.md